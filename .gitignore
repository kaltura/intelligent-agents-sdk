# macOS noise
.DS_Store

# Credentials (never commit AGENTIC_PARTNER_ID / AGENTIC_ADMIN_SECRET)
.env
.env.*

# Dependencies (this package is zero-dep, but devDeps of a future contribution env)
node_modules/

# Logs
*.log

# Editor
.vscode/
.idea/

# Test reports (generated by `npm run test:ci`, uploaded as a CI artifact)
test-results.junit.xml

# Harness run output (scripts/harness/run.mjs) — regenerated every run, never committed
.harness-output/

# Live-verify run artifacts (scripts/live-verify.mjs) — uploaded as a CI artifact, never committed
live-verify-artifacts/

# Claude Code agent worktree scratch space — regenerated per run, never committed
.claude/worktrees/
