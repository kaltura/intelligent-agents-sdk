{
  "name": "intelligent-agents-sdk-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('_site',{recursive:true,force:true})\" && eleventy",
    "generate": "node scripts/generate-docs.mjs && npm run build && node scripts/check-anchors.mjs && node scripts/check-sections-manifest.mjs && node scripts/check-manifest-coverage.mjs && node scripts/check-sdk-pin-sync.mjs",
    "pretest": "npm run build",
    "test": "node scripts/check-anchors.mjs && node scripts/check-sections-manifest.mjs && playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@playwright/test": "^1.48.0",
    "markdown-it-anchor": "^10.0.0"
  }
}
